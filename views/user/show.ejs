<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>`${user.id} Plan for the Day`</title>
    </head>
    <body>
        <section class="hero is-primary has-text-centered">
            <div class="hero-body">
              <p class="title">Itenarary for <%= travelplan.location %> on<%= travelplan.tripDate %> </p>
            </div>
            <div><%= travelplan.subHeading %></div>
            <div><%= travelplan.notes %></div>
            <div><%= travelplan.notes %></div>
            <div><%= travelplan.address %></div>
            <nav>
                <a href="/user/index">🌏 Return to Vacation Itenarary 🌎</a>
            </nav>
        </p>
    </body>
</html>